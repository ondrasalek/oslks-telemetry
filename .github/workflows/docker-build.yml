name: Build and Push Docker Images

on:
    push:
        branches:
            - main
        paths:
            - 'backend/**'
            - 'frontend-react/**'
            - 'docker-compose.yml'
            - '.github/workflows/docker-build.yml'

jobs:
    build-and-push:
        runs-on: ubuntu-latest
        permissions:
            contents: read
            packages: write

        strategy:
            matrix:
                service: [collector, frontend]
                include:
                    - service: collector
                      context: ./backend
                      image: ghcr.io/${{ github.repository_owner }}/oslks-collector
                    - service: frontend
                      context: ./frontend-react
                      image: ghcr.io/${{ github.repository_owner }}/oslks-frontend

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Set up QEMU
              uses: actions/setup-qemu-action@v3

            - name: Set up Docker Buildx
              uses: actions/setup-buildx-action@v3

            - name: Log in to GitHub Container Registry
              uses: actions/login-action@v3
              with:
                  registry: ghcr.io
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Build and push ${{ matrix.service }}
              uses: docker/build-push-action@v5
              with:
                  context: ${{ matrix.context }}
                  platforms: linux/amd64,linux/arm64
                  push: true
                  tags: |
                      ${{ matrix.image }}:latest
                      ${{ matrix.image }}:${{ github.sha }}
                  cache-from: type=gha
                  cache-to: type=gha,mode=max
                  build-args: |
                      VITE_APP_URL=${{ vars.VITE_APP_URL }}
                      VITE_OSLKS_COLLECTOR_URL=${{ vars.VITE_OSLKS_COLLECTOR_URL }}
                      VITE_OSLKS_WEBSITE_ID=${{ vars.VITE_OSLKS_WEBSITE_ID }}
